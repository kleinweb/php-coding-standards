ignore_merge_commits = true
post_bump_hooks = [
  "git push",
  "git push {{version}}",
]
pre_bump_hooks = [
  "composer validate",
]

[changelog]
owner = "kleinweb"
path = "CHANGELOG.md"
remote = "github.com"
repository = "php-coding-standards"
template = "remote"
